public class CompareNumbers {
    public static int compareTwoNumbers(int x, int y) {
        if (x > y)
            return 1;
        else if (x == y)
            return 0;
        else
            return -1;
    }
}